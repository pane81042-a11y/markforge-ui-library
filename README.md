# markforge-ui-library
Shared UI component library for MarkForge built with React. Provides reusable components, design tokens, layout primitives, and styling consistency across all MarkForge microfrontends. Published as a private package and integrated via shared dependencies.
